streamlit==1.32.2
python-dotenv==1.0.1

# Voice
SpeechRecognition==3.10.1
audio-recorder-streamlit==0.0.8

# LangChain ecosystem (carefully pinned to avoid breaking changes)
langchain==0.1.20
langchain-community==0.0.38
langchain-text-splitters==0.0.1
langchain-google-genai==1.0.3
langchain-pinecone==0.0.3

# Embeddings & NLP
sentence-transformers==2.6.1
tiktoken==0.7.0

# Pinecone (new SDK but stable)
pinecone==3.2.2

# Document loaders
pypdf==4.2.0
python-docx==1.1.0
docx2txt==0.8

# Optional but required internally by some loaders
unstructured==0.13.7